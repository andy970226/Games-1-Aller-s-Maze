using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Tranprt : MonoBehaviour {

	// Use this for initialization
	void Start () {
        this.GetComponent<Renderer>().material.color = new Color(0.5f, 0.8f, 0.1f, 0.5f);
    }
	
	// Update is called once per frame
	void Update () {
		
	}
}
